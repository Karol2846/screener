spring:
  application:
    name: screener

  datasource:
    url: "jdbc:postgresql://localhost:5432/screener"
    username: "screener"
    password: "password"
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate